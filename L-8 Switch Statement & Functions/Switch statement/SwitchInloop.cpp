#include<iostream>
using namespace std;

int main(){

    int num=1;

    while (true)
    {
        switch(num){
            case 1: cout<<"num is 1"<<endl;
            break;
        }
        exit(0);
    }
    
}